pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven { url 'https://jitpack.io' } // Keep JitPack for other potential dependencies
        maven { url 'https://dl-xposed.xroche.com/maven/' } // Add official Xposed repository
    }
}

rootProject.name = "mfdc_xposed_module"
include ':app'
